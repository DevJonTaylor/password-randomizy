{
  "length prompt": [
    {
      "description": "If the user selects cancel regardless of the number input the return value is null.  This will be converted to NaN and will throw an Error.",
      "input": [["0", "pushed Cancel"],["", "pushed Cancel"], ["20", "pushed Cancel"]],
      "expected": "Stops the event.",
      "results": "Stops the event."
    },
    {
      "description": "If the user provides a number less then 8 or more then 128 it is outside the requirements.",
      "input": [0,4,5,129,20000],
      "expected": "Stops the user and alerts them then repeats the prompt.",
      "results": "Stops the user and alerts them then repeats the prompt."
    },
    {
      "input": [8, 20, 32, 128],
      "expected": "Accepts the user input and moves to the next parts.",
      "results": "Accepts the user input and moves to the next parts."
    }
  ],
  "characters confirm": [

  ]
}